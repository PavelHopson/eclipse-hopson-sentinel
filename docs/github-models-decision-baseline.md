# GitHub Models decision baseline — retired

This document is retained only as historical context.

GitHub retired GitHub Models, including the model catalog and inference API, on 2026-07-30.
The Sentinel Decision Baseline Evidence workflow must not use `models.github.ai`.

The active baseline path is documented in:

```
docs/openai-decision-baseline.md
```

Historical workflow runs against GitHub Models are diagnostic-only and must never be used as
baseline evidence or candidate-comparison input.
