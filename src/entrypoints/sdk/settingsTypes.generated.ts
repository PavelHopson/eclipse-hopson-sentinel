import type { SettingsJson } from '../../utils/settings/types.js'

/**
 * Public SDK settings contract.
 *
 * The source snapshot omitted the generated alias; the runtime SettingsSchema
 * remains the canonical definition through SettingsJson.
 */
export type Settings = SettingsJson
